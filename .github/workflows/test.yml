name: Test GitHub Actions

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test echo
        run: echo "GitHub Actions is working!"
      
      - name: Show date
        run: date